// Lo dejamos vacío, devolviendo null.
// De esta manera no rompes las importaciones de tu proyecto, 
// pero matamos los cuadros feos.

export default function PublicCoreDecisionRail({ locale, variant }: { locale?: string, variant?: string }) {
  return null;
}